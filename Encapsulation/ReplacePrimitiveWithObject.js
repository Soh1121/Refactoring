class Order
{
    constructor(data) {
        this.priority = data.priority;
        // 初期化が続く
    }
}

// client...
highPriorityCount = orders.filter(0 => "high" === o.priority
                                    || "rush" === o.priority)
                            .length;
